# Write your MySQL query statement below
select c.customer_id,count(c.visit_id) as count_no_trans from Visits as c left join Transactions as t on t.visit_id=c.visit_id where t.transaction_id is null group by c.customer_id having count_no_trans>0 